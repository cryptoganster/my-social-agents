/**
 * Shared Event Infrastructure
 *
 * Concrete implementations of event publishing infrastructure
 * used across all bounded contexts.
 */
export { EventPublisherService } from './event-publisher';
